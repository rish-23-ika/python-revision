#3. Polymorphism in Functions
#Problem:
#Write a function `multiply` that multiplies two numbers, but can also accept and multiply strings.

def multiply(a,b):
    return (a * b)

print(multiply(5, 3))
print(multiply("hlo ", 4))

#we are not taking input from user here as for this particular it will show error
